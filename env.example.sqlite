## Example env for SQLite mode (Optional)

# Server Configuration
PORT=5000
NODE_ENV=development

# Client URL (for CORS)
CLIENT_URL=http://localhost:3000

# JWT Secret (Change this in production)
JWT_SECRET=your-secret-key-change-in-production

# Notes:
# - SQLite uses local file `payroll.db` in project root.
# - DB_* variables are not needed for SQLite.

