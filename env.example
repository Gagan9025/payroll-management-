# Example .env for MySQL mode (Optional)

# Database Configuration (MySQL only)
DB_HOST=localhost
DB_USER=root
DB_PASSWORD=your_mysql_password
DB_NAME=payroll_management

# JWT Secret (Change this in production)
JWT_SECRET=your-super-secret-jwt-key-change-in-production

# Server Configuration
PORT=5000
NODE_ENV=development

# Client URL (for CORS)
CLIENT_URL=http://localhost:3000

# Notes:
# - SQLite is the default and does not require DB_* variables.
# - If `.env` is missing, the SQLite server uses defaults for PORT, CLIENT_URL, and JWT_SECRET.
